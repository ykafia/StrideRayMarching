using Stride.Engine;

namespace VolumetricShader
{
    class VolumetricShaderApp
    {
        static void Main(string[] args)
        {
            using (var game = new Game())
            {
                game.Run();
            }
        }
    }
}
